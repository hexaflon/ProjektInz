@page
@model TestTest.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
    
}

<h1>Index</h1>
@{if(ViewData["message"]!=null){
    <h2>
        @ViewData["message"];
    </h2>
}
}
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Haslo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User[0].Imie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.User) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Haslo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imie)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdOsoba">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdOsoba">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdOsoba">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
